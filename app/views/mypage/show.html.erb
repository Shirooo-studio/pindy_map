<div id="map-container" class="relative w-full h-[calc(100dvh-0px)]">
  <!-- 地図 -->
  <div id="map" class="absolute inset-0"></div>

  <!-- 右パネル：検索パネルと同じCSSを再利用するため id=search-panel を採用 -->
  <aside id="search-panel" class="search-panel hidden" aria-label="マイページパネル">
    <div id="me-panel" class="p-3 space-y-3">

      <!-- プロフィールカード -->
      <section class="bg-white p-3 space-y-3">
        <div class="flex items-center gap-3">
          <% avatar_src = current_user.avatar.attached? ? url_for(current_user.avatar) : nil %>
          <div class="flex-1">
            <div class="font-medium" id="pf-display-name">-</div>
            <div class="text-xs text-gray-500">@<span id="pf-username">-</span></div>
          </div>
          <button id="profile-edit-btn" class="px-3 py-1.5 rounded bg-blue-600 text-white text-sm">
            プロフィールを編集
          </button>
        </div>

        <div class="grid grid-cols-4 gap-2 text-center text-sm">
          <div><div class="font-semibold" id="pf-count-posts">0</div><div class="text-gray-500">投稿</div></div>
          <div><div class="font-semibold" id="pf-count-pins">0</div><div class="text-gray-500">ピン</div></div>
          <div><div class="font-semibold" id="pf-count-followers">0</div><div class="text-gray-500">フォロワー</div></div>
          <div><div class="font-semibold" id="pf-count-following">0</div><div class="text-gray-500">フォロー中</div></div>
        </div>

        <div class="text-sm text-gray-700 leading-relaxed" id="pf-bio">-</div>
        <div class="text-xs text-gray-500">会社：<span id="pf-company">-</span> ／ 部署：<span id="pf-dept">-</span></div>
      </section>

      <!-- タブ＆中身：JSがここにリストを描画します -->
      <div class="mt-4">
        <div class="flex gap-4">
          <button id="tab-btn-posts">投稿</button>
          <button id="tab-btn-pins">ピン</button>
        </div>
        <div id="tab-posts">
          <ul id="my-posts" class="space-y-4"></ul>
        </div>
        <div id="tab-pins" class="hidden">
          <ul id="my-pins" class="space-y-2"></ul>
        </div>
      </div>
    </div>
  </aside>

  <div id="detail-panel" class="detail-popup hidden">
    <div id="detail-body" class="p-0"></div>
  </div>
</div>